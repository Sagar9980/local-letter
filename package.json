{
  "name": "local-letter",
  "private": true,
  "description": "Transactional email that speaks every language — self-hosted template editor, translations, and rendering API.",
  "license": "MIT",
  "homepage": "https://github.com/Sagar9980/local-letter#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Sagar9980/local-letter.git"
  },
  "bugs": {
    "url": "https://github.com/Sagar9980/local-letter/issues"
  },
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma"
    ]
  }
}
