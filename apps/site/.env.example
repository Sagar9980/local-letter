# Optional. When set, the contact form POSTs the enquiry as JSON to this URL.
# Leave it unset and the form opens a pre-filled draft in the visitor's mail
# client addressed to the sales address in lib/site.ts instead.
NEXT_PUBLIC_CONTACT_ENDPOINT=""
